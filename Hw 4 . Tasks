#найти сумму ряда чисел от 0 до 100

x = 100
c = 0
summa = 0
while c < 100:
    c+= 1
  
    summa+=c

print('Сумма чисел от 0 до 100 равна :')
print(summa)

~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
#почситать сумму числового ряда от 0 до 14

x =14
c =0
summa = 0 
while c< 14:
    c+= 1
    summa+=c
print(summa)


~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

#почситать сумму числового ряда от 0 до  n. n вводиться пользователем
print(   'Привет! Давайте поиграем в игру. Введите число а я вам посчитаю сумму от 0 до вашего числа')
n =  int(input())
c = 0
summa = 0
while c < n:
    c+= 1
    summa +=c
  
print('Сумма равна', + int(summa))
print('Можешь проверить на калкуляторе если че)')


~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

# Написать программу которая будет складывать, вычитать,умножать и делит на два числа

print('Привет! Я твой помошник по матиматике, ты мне говоришь числа и что надо с ними делать а я тебе ответ, попробуем?)\n Если вдруг заскучает нажимай ноль в качестве знака операции , так ты остановишь программ')
while True:
    s = input('Знаки операции(+,*,-,/)= ')
    if s == '0': break
    if s in ('+','*','-','/'):
        x = float(input('x='))
        y = float(input('y='))
        if s == '+':
              print(x+y)
        elif s == '*':
            print(x*y)
        elif s == '-':
            print(x-y)
        elif s == '/':
            if y != 0:
                print(x/y)
            else:
                print('Дурачок делить на ноль нельзя')
    else:
         print('Введи корректный знак операции')
